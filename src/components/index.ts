// Shared app-level components that compose the design system into product chrome. These are not
// design-system primitives; they are specific to this web client.
export { AppShell } from './AppShell';
export { RoleGuard } from './RoleGuard';
export { RoleSwitcher } from './RoleSwitcher';
